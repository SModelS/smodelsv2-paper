# Including pion decays into the IDM or Scoto implementations

`decayScan.c` is the micrOMEGAs main used to generate `pionDecayScan.csv`.  
`plotPion.ipynb` is the notebook plotting the width vs mass splitting
